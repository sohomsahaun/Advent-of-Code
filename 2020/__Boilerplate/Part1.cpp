#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int solve(vector<int>& v) {

}

int main() {
    freopen("in1.txt", "r", stdin);

    int n;
    vector<int> input;
    while (cin >> n) {
        input.push_back(n);
    }

    cout << solve(input);

    return 0;
}
