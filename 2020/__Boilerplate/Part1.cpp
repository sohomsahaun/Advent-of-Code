#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int solve(vector<string>& v) {

}

int main() {
    freopen("in1.txt", "r", stdin);

    string s;
    vector<string> input;
    while (cin >> s) {
        input.push_back(s);
    }

    cout << solve(input);

    return 0;
}
