#include <bits/stdc++.h>

using namespace std;
using ll = long long;

vector<string> in;

int solve() {

}

int main() {
    freopen("in2.txt", "r", stdin);

    stringstream str;
    string s, ss;

    while (getline(cin, s)) {
        str = stringstream(s);

    }

    cout << solve();

    return 0;
}
